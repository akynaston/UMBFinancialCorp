Installation

1. Copy ACEShim.jar and jace.jar to the IDM library directory
2. Copy libjace_api.so to the library path specifed in 'LIBPATH'.
3. Import the driver export, RSADriver.xml, into eDirectory.

Note: Ensure that the driver is configured with the user name and password of an admin user in the RSA/ACE system.
